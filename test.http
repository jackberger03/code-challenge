### Create a signing session (tests create_envelope_from_template under the hood)
POST http://localhost:8000/create-signing-session
Content-Type: application/json

{
    "email": "john.doe@example.com",
    "name": "John",
    "role_name": "Hiring Manager",
    "phone": "5125551234"
}

###


# Health check (optional, confirms the backend is running)
GET http://localhost:8000/health